# esp32-ile-tft-ekran
bu projede esp32 cam kullanarak tft ekrana anlık görüntü aktarımı yaptık
bağlantılar(benim kullandığım ekran için)
SCL (SCK): GPIO 14

SDA (MOSI): GPIO 13

RES (Reset): GPIO 12

DC (Data/Command): GPIO 2
